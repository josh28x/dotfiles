#use bash (not sh, etc)  as the default shell for new tmux windows
set-option -g default shell /bin/bash  

#enable mouse scrolling
set-option -g mouse on

#let tmux integrate copied text with the terminal clipboard
set-option -s set-clipboard on

